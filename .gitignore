# --- Node / TypeScript (core) ---
node_modules/
dist/
*.tsbuildinfo

# --- Prisma: regenerated by `npm run db:generate` (and postinstall) ---
core/src/generated/

# --- Python (legacy services; kept so stray artifacts never re-enter) ---
venv/
.venv/
__pycache__/
*.py[cod]

# --- Rust (sandbox) ---
target/

# --- Secrets ---
.env
.env.*
!.env.example

# --- Logs & coverage ---
*.log
npm-debug.log*
logs/
coverage/

# --- Local tooling / OS ---
.claude/settings.local.json
.DS_Store

# --- Prisma-installed agent skills (kept on disk, not published) ---
**/.agents/
**/.windsurf/
**/.claude/skills/
**/skills-lock.json

# --- Local design/working notes, not published ---
DESIGN.md
